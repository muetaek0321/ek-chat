# AGENTS.md

このリポジトリでコードを生成・修正する際は、以下の共通ルールに従ってください。
各領域の固有ルールは、`frontend` および `backend` 配下の `AGENTS.md` を参照してください。

## ルールの適用

- 作業対象のファイルを変更する前に、リポジトリ直下および対象ディレクトリにある `AGENTS.md` を確認する。
- 子ディレクトリの `AGENTS.md` は、この共通ルールを補完する。内容が競合する場合は、より対象に近いルールを優先する。
- ユーザーからの明示的な指示がある場合は、その指示を優先する。

## 作業範囲の保護

- 作業開始前に `git status` と必要な差分を確認し、既存の未コミット変更はユーザーの作業として扱う。
- 依頼と無関係なファイル、設定、既存の変更を上書き・削除・整形しない。
- 依存関係を変更する場合は必要性を明確にし、使用するパッケージマネージャーのロックファイルも整合させる。

## 基本方針

- 可読性を最優先にする。
- シンプルで保守しやすい実装を優先する。
- 必要以上に複雑な実装や、要求されていないリファクタリングを避ける。
- 既存の設計・命名規則・コーディングスタイルを尊重する。
- 変更範囲は必要最小限にする。
- 既存の公開 API との互換性を可能な限り維持する。
- サンプルコードや省略形ではなく、そのまま実行できるコードを生成する。
- 新しいライブラリの追加は、本当に必要な場合のみ行う。

## コメントとドキュメント

- コードコメントや説明文は原則として日本語を使用する。
- コメントは必要最小限にし、「何をしているか」ではなく「なぜそうするか」の意図や理由を補足する。
- TODO コメントを追加する場合は理由も記載する。
- 既存のコードや API と実装がずれないよう、必要に応じてドキュメントを更新する。

## エラーハンドリングとセキュリティ

- 想定される例外やエラー状態は適切に処理する。
- エラーメッセージは原因が分かる具体的な内容にする。
- 秘密情報（API キー、トークン等）をコードへ埋め込まない。
- 外部入力は信頼せず、必要に応じて検証する。
- 危険なデシリアライズを避ける。

## テストと変更確認

- コマンド実行、検証、テストは対象ディレクトリ（`backend/` または `frontend/`）に移動して実行する。
- 既存機能を変更した場合は、必要に応じて関連テストを更新する。
- 新しい処理は可能な限りテストしやすい設計とする。
- 変更後は、対象ディレクトリで利用可能な formatter、linter、type checker、test を実行する。
- ソースコードを変更し得る formatter は、可能な限り変更対象のファイルに限定し、実行後に意図しない差分がないことを確認する。
- 検証を実行できない場合は、未実行のコマンド、理由、想定される影響を明確に報告する。

## Git

- 関係のないファイルを変更しない。
- 不要なコードや依存関係を追加しない。
- 依頼されない限り、コミットやブランチ作成を行わない。

## Agent Skills

- 利用可能なスキルは `.agents/skills/<スキル名>/SKILL.md` を確認する。
- ユーザーがスキル名を指定した場合、または依頼内容がスキルの `description` に一致する場合は、作業前に該当する `SKILL.md` を最後まで読み、その手順に従う。
- `SKILL.md` が参照する追加資料は、作業に必要なものを確認してから利用する。
- スキルは作業手順を補助するものであり、ユーザーの指示および対象ディレクトリの `AGENTS.md` より優先しない。
- 該当するスキルが見つからない、または現在の実装と矛盾する場合は、その旨を報告し、既存のルールに従って作業する。

---

複数の実装方法がある場合は、**可読性、保守性、安全性、テスト容易性、性能、コード量** の順に優先する。
